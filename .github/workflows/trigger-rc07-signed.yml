name: Trigger BLOFY RC07 Signed Release

on:
  push:
    branches: [rc07-commercial-stability]

permissions:
  contents: read

jobs:
  trigger-signed:
    uses: ./.github/workflows/rc07-release.yml
    secrets: inherit
