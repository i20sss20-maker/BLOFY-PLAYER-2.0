BLOFY PLAYER 2.0 rc05 release gate

Final signed candidate:
- full professional TV UI polish merged to main
- resilient multi-source poster fallback with retry, memory/disk cache and prefetch
- real rotating home banners with artwork fallback and smoother transitions
- unified TV typography, spacing, rounded cards, focus glow and motion across key screens
- improved live TV category/channel layout, larger preview and clearer D-pad focus
- movie and series detail screens polished with unified typography/buttons and artwork fallback
- TV settings visually rebuilt while retaining current functional options
- instant search from the first character across live, movies and series
- cached catalog navigation to avoid reloading lists on every entry
- series episodes use local cache first and fetch only when missing
- external player support removed from UI, manifest and code paths
- parental lock/PIN feature removed and legacy locked state cleared by migration
- local storage management with safe cache cleanup added to settings
- silent long-running live stall recovery added without changing playback engine, URL or format
- private «مشتركين BLOFY» login added with username/password only and upstream host kept in backend environment
- current Media3 1.6.1 + FFmpeg playback engine, transport, URL resolution and fallbacks retained
- Android CI, Activation CI and native FFmpeg verification passed before final signed build
- production activation backend verified healthy after subscriber proxy deployment
- production signed RC05 rebuild requested from final main commit 10927d6278c5e97cde12e68119ea476853a739d5
