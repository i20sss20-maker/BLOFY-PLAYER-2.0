# مراجعة استقرار BLOFY PLAYER 2.0 قبل الإصدار التجاري

## نقطة البدء

- الفرع المعتمد: `rc07-commercial-stability`.
- نسخة اختبار الجهاز: `2.0.0-rc07.9` / `2000017`.
- المصدر المعتمد: `72434a500946286b84e9c779fb44e02d6052144c`؛ طابقت ملفات المصدر الـ307 بصمات Git دون اختلاف.
- Android CI #1208: 275 اختبارًا ناجحًا، دون فشل أو تخطٍ. الإصدار الموقّع #257 ناجح.
- آخر Activation CI قبل المراجعة: 42 اختبارًا واختبارات التكامل ناجحة على الـCommit الأب؛ آخر تعديل في النسخة المعتمدة كان اختبار Android فقط.
- فحص الإنتاج المباشر بتاريخ 2026-09-07 أعاد HTTP 200 و`database=ready` و`playlistEncryption=ready` ونفس الـCommit المعتمد.

العمل معزول في `audit/rc07-commercial-cleanup` وطلب المراجعة #36. إصدار APK الجديد والنشر الإنتاجي والدمج ليست نتائج منفذة لهذه المراجعة.

## الإصلاحات

| المجال | المشكلة المثبتة والتصحيح |
|---|---|
| سلامة الكتالوج | التحديث اليدوي لم يطبق حماية اكتمال الأقسام. أصبحت الترقية من البيانات المؤقتة تتحقق من أعداد Live/Movies/Series داخل معاملة Room وتحافظ على المكتبة المعتمدة عند النقص. |
| البحث | تطبيق LIMIT قبل نوع المحتوى كان يفوّت نتائج عربية صحيحة عند كثرة التطابقات. انتقل قيد النوع إلى استعلام FTS قبل LIMIT، وأصبح البحث السابق يُلغى فعليًا عند إدخال بحث أحدث. |
| ترحيل FTS | ترحيل قاعدة البيانات 9 إلى 10 يمسح الفهرس مع بقاء علامة جاهزية v9. أصبح التحقق مرتبطًا بعلامة v10 ووجود الفهرس الفعلي قبل إعلان الجاهزية. |
| استعادة التحميل | بقايا استيراد أول انقطع بإغلاق العملية كانت قابلة للاعتماد كمكتبة كاملة. تُزال دفعات الاستيراد التي لم تعتمد فقط، مع الحفاظ على المكتبة السابقة الجاهزة. |
| تبديل مصدر القائمة | تُبطل بيانات Home والبيانات الوصفية القديمة بعد اعتماد المصدر البديل؛ تغييرات البوابة تُجهز بصورة مرحلية وتحافظ على المصدر المعتمد حتى نجاح البديل. اختيار المزود ومزامنة البوابة يتسلسلان لمنع رد قديم من إلغاء اختيار أحدث. |
| بيانات المزود | فشل قراءة Keystore كان يعرض حقولًا فارغة قد تُكتب فوق بيانات مشفرة قابلة للاستعادة. تحافظ معاملة الحفظ على النص المشفر عند كتابة تلك القراءة الناقصة. |
| حماية PIN | حماية تغيير وحذف الرموز وإدارة ملفات Kids، ومنع المدخلات غير الصحيحة من إزالة رمز موجود. دعم PBKDF2-HMAC-SHA256 بنفس التنسيق على Android 6/7 دون تخفيض الخوارزمية. |
| المزامنة الشخصية | تسلسل جميع طلبات المزامنة وحفظ تعديلات المستخدم التي تتم أثناء الشبكة، بما فيها الحذف والإعدادات، مع الاحتفاظ بإضافات الخادم غير المتعارضة. |
| الرئيسية والذاكرة | معرفات غير مرئية وثابتة لترتيب الصفوف عبر اللغات، واستعادة الصفوف والبطاقات عند تبديل الملف، وتطبيق فلترة Kids على المحتوى المتأخر، وإيقاف المؤقتات خارج الشاشة. كاش الصور يُفرغ عند ضغط الذاكرة، وكاش الروابط المتعطلة محدود الحجم. |
| جاهزية الخدمة | إصلاح عمود خطأ في استعلام الخطط، ومطابقة الحد الأدنى لسر الدفع مع المعالج الفعلي. |
| مصادقة الخدمة | محاولات PIN الفاشلة والقفل محددان بصورة مشتركة ومستمرة بين مسارات الحساب والسحابة والتجديد المراجعة، مع رد 429 مناسب. |
| لوحة الإدارة | إصلاح ترتيب المصادقة بالجلسة، واستبدال إدراج بيانات العملاء داخل HTML بعقد نصية آمنة. |
| كتابة السحابة | تسلسل أول كتابة وإعادة الاستعادة بقفل صف الجهاز لمنع الكتابتين المتزامنتين من اعتماد نفس رقم المراجعة. |
| الدفع والاشتراكات | الحفاظ على حظر الجهاز والاشتراك الدائم، ومنع إشعار قديم من إعادة تفعيل طلب مسترد، وتحرير حجوزات القسائم مرة واحدة، والتحقق من المبالغ الصحيحة، وتسلسل تمديدات الجهاز، ومنع تمديد طلب Zid نفسه مرتين. حالة الاشتراك تعكس الاستحقاق الفعال. |
| استجابة الخادم | منع كتابة استجابة ثانية بعد انتهاء الرد، والتعامل مع الاستثناء بعد إرسال Headers دون محاولة إرسال Headers جديدة. |
| إنشاء قاعدة جديدة | كشفت سجلات CI محاولة إنشاء جدول العملاء قبل جدول الأجهزة مع تجاهل الخطأ. انتقل تعريف الجدول إلى المخطط المشترك الذي يُنتظر اكتماله قبل استقبال الطلبات، مع اختبار جديد لقاعدة فارغة. |

## التنظيف المثبت

أزيلت نسخ الفوكس والقائمة السريعة والتخزين الشخصي القديمة غير المستخدمة، وواجهات تحميل خلفية لا تنفذ عملًا، واستعلامات DAO غير مستدعاة، ومحاولة قديمة لإعادة بيانات Headers إلى URL، والاختبارات التي كانت تختبر الأكواد المحذوفة فقط. بقيت أسماء Workers القديمة اللازمة لحل مهام WorkManager المحفوظة على أجهزة المستخدمين.

لم تُحذف موارد التصميم لمجرد تحذير Lint. التقرير القديم يحتوي 621 تحذيرًا، منها 118 موردًا غير مستخدم مرشحًا للمراجعة؛ التحذير لا يبرر تغيير الثيم. إضافات موارد Android الجديدة تقتصر على معرفات View غير مرئية.

صُححت وثائق النسخة والفرع وشهادة التوقيع؛ الشهادة الفعلية والمفتاح ومحركات التشغيل لم تتغير.

حُفظ ملف تثبيت تبعيات الخادم من تثبيت CI ناجح واستخدم `npm ci` بدل حل إصدارات عائمة عند كل بناء.

## حدود محمية تحتاج قرار المالك

لا تزال ملاحظات داخل `subscriber-proxy-hook.mjs` خارج نطاق التعديل المأذون:

1. سرية بيانات المصدر داخل روابط HLS الموقعة ليست كافية؛ أثبت فحص ببيانات وهمية إمكانية استعادة بيانات المصدر من الرابط.
2. خطأ أثناء قراءة بث المصدر قد يصل إلى العملية كخطأ غير معالج بدل إنهاء الطلب المنفرد.
3. مصادقة جلسة المشترك لا تستخدم حماية المحاولات الفاشلة التي أضيفت للمسارات الأخرى.

يوجد أيضًا استمرار لمعالجة بعض الطلبات بعد إرسال الرد في طبقة البروكسي؛ حارس استجابة الخادم يخفف الرد المزدوج، دون تغيير منطق البروكسي المحمي. لا يمكن اعتماد الجاهزية التجارية الكاملة قبل حسم الملاحظات المحمية واختبار الجهاز.

الفحص الساكن لمسارات Android المحمية أظهر نقاطًا تحتاج اختبارًا وإذن تعديل مستقلًا: احتمال فقد موضع الاستئناف عند fallback، وعدم حفظ موضع الحلقة قبل الانتقال اليدوي، وتعديل صيغة رابط M3U في تفضيل البث، وعدم ترميز الرموز المحجوزة في بيانات دخول رابط Catch-up. لم تُغيّر هذه المسارات، ولم يُدّعَ إثبات أثرها على جهاز المستخدم.

ظهرت في السجلات أخطاء Headers وTimeout مرتبطة بنشر أقدم؛ لا تُنسب تلقائيًا للـCommit الحالي. فحوص الأخطاء المذكورة أعلاه استخدمت مصادر وهمية، دون بيانات عميل أو تغيير في الإنتاج.

## التحقق والتسليم

اختبارات الانحدار تشمل Room وترحيل قاعدة البيانات وإعادة فتحها، وسباقات المزامنة والإلغاء، وحماية PIN، وضغط الذاكرة، ومحاسبة الدفع. أضيفت اختبارات HTTP مع PostgreSQL حقيقي مع منع تشغيلها على عناوين خارج loopback، للتحقق من القفل والمزامنة والتجديد المتزامن وحالات الدفع والقسائم.

البيئة المحلية لا توفر Gradle أو Android SDK، لذا تنفيذ اختبارات Android يعتمد على CI. نتائج CI النهائية موثقة في طلب المراجعة. لا تعني اختبارات المحاكاة نجاح التشغيل على جهاز حقيقي.

أول نقطة تحقق للخادم على فرع المراجعة اجتازت Activation CI #241: 76 اختبارًا واختبارات PostgreSQL الفعلية وSmoke ومزامنة البوابة. كشفت مراجعة السجلات بعدها مشكلة إنشاء الجدول المذكورة أعلاه، وأضيف اختبار يحولها إلى فشل صريح بدل الاكتفاء بنتيجة CI الخضراء.

قبل APK جديد: زيادة النسخة، نجاح Android وActivation CI والإصدار الموقّع على الفرع المسموح، والتحقق من شهادة الإنتاج والتثبيت فوق النسخة الحالية دون حذف البيانات. بقي قيد فرع التوقيع سليمًا.

اختبار الجهاز المتبقي: Hidden Host، قوائم M3U/Xtream الضخمة، اكتمال الأقسام، إعادة فتح القوائم، سرعة الفئات، البحث، الذاكرة المنخفضة وفوكس الريموت. ويجب اختبار إنشاء وفحص PIN على Android 6/7 فعليًا لقياس توافق وأداء مزود التشفير.
